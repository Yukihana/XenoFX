/* The base common library should not have:
 * - Third-Party Dependencies
 *
 * However, micro-library dependencies are allowed, such as:
 * - FileGlobbing
 * - Abstractions
 */

// TODO:

/* Create a separate Validations library:
 * - Move MimeDetective and MimeTypesMap related code there.
 */

/* Move module abstractions to dedicated library */