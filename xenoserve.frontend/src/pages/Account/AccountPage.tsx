export default function AccountPage() {
    return (
        <section>
            <h2>Account Page</h2>
            {/* Login will go here, will redirect to profile on logged in */}
        </section>
    );
}